# Frontend (GitHub Pages)

1) Update `config.js` with your API base URL.
2) Deploy the `frontend` folder to GitHub Pages.

The site is a static app that calls the FastAPI backend directly (no auth or history layer).
